generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Project {
  id          String   @id @default(uuid())
  name        String
  description String?
  framework   String
  status      String
  slug        String   @unique
  createdAt   DateTime @default(now())
}